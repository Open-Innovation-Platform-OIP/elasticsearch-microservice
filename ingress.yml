apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-es-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-es-dev.k8s.jaagalabs.com
    secretName: oip-es-dev-tls
  rules:  
  - host: oip-es-dev.k8s.jaagalabs.com
    http:
      paths:
      - backend:
          serviceName: oip-es-dev
          servicePort: 80