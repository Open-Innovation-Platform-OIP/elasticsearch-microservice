apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-es-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-es-dev.oip.dev.code-alpha.org
    secretName: oip-es-dev-tls
  rules:  
  - host: oip-es-dev.oip.dev.code-alpha.org
    http:
      paths:
      - backend:
          serviceName: oip-es-dev
          servicePort: 80